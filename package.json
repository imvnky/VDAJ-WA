{
  "name": "vdajservices-whatsapp-platform",
  "version": "1.0.0",
  "description": "VDAJ Services — WhatsApp Bulk Messaging SaaS. Root workspace launcher.",
  "private": true,
  "scripts": {
    "dev":           "concurrently --names \"BACKEND,FRONTEND\" --prefix-colors \"magenta,cyan\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend":   "npm run dev --prefix backend",
    "dev:frontend":  "npm run dev --prefix frontend",
    "install:all":   "npm install --prefix backend && npm install --prefix frontend",
    "build":         "npm run build --prefix frontend",
    "seed":          "npm run seed --prefix backend",
    "migrate":       "node backend/src/database/migrate.js"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
